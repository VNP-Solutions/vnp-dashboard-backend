/**
 * Kept as a re-export: portfolio, property and sync-action-log imported a second copy of this
 * guard, so hardening one left the other accepting browser tokens.
 */
export { ExternalJwtGuard } from '../../external-communication/guards/external-jwt.guard'
